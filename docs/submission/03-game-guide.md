# 게임 소개 및 설명 문서

> **제출물 3** · 초안 · 확정 후 PDF로 export  
> `TODO` 표시는 제출 전 반드시 채울 항목입니다.

---

## 표지

| | |
|--|--|
| **게임 제목** | 갤럭시 메이헴 (Galaxy Mayhem) |
| **프로젝트명** | Project Afterburn |
| **한 줄 소개** | XP로 살아남을수록 적도 강해지는, 증강 선택형 종스크롤 슈팅 |
| **팀** | team-sema |
| **엔진** | Godot 4.7 |
| **플랫폼** | 웹 브라우저 (GitHub Pages) / 소스 실행 |

---

## 1. 게임 개요

**갤럭시 메이헴**은 세로 스크롤 슈팅에 **플레이어 강화 ↔ 적 강화** 증강 선택을 얹은 프로토타입입니다.

적을 처치하고 경험치(XP)를 모아 레벨업하면 플레이어 강화를, 일정 시간이 지나면 적 강화를 고르게 됩니다. 핵심 재미는 **내 빌드를 키우는 동시에, 적의 강화 방향까지 고르는** 데 있습니다.

플레이어 강화는 두 갈래입니다.

- **함선 모듈:** 시설 효과는 범용 슬롯에 장착되고, 슬롯이 차면 tag와 관계없이 무엇을 빼고 넣을지 고릅니다.
- **무기:** 레벨업 선택 화면에서 새 무기를 얻거나, 이미 가진 무기의 전용 모듈을 강화한다. 함선 모듈용 육각 슬롯은 쓰지 않고 STATUS의 **무기 모듈** 영역에 들어가며, 장착한 무기는 모두 함께 자동 발사된다.

### 핵심 루프

1. 적 처치 → 점수·XP 오브 획득  
2. XP로 레벨업(**C**) → 3지선다: 함선 모듈 장착·슬롯 확장, 또는 무기 획득·전용 모듈 강화 (일시정지)
3. 약 30초마다 **적 강화** 3지선다 (일시정지)  
4. 시설 슬롯이 차면 기존 모듈을 교체하며 빌드를 다듬고, 무기 모듈도 만석이면 교체(피교체 무기 성장 삭제)  
5. 피격으로 함선 파괴 → 게임 오버 → 하이스코어 확인 → 메뉴

승리(스테이지 클리어) 조건은 없고, **생존·고득점**이 목표입니다.

---

## 2. 플레이 방법

### 목표

- 가능한 오래 생존하며 점수를 쌓는다.
- 함선 모듈과 무기 모듈로 빌드를 키운다.
- 하이스코어를 갱신한다.

### 조작

| 입력 | 동작 |
|------|------|
| 방향키 또는 **WASD** | 함선 이동 |
| (자동) | 장착 무기 **자동 발사** |
| **C** | XP 준비 시 플레이어 증강 오픈 |
| **ESC** | 수동 일시정지 토글 |
| 스페이스 (또는 Enter) | 메뉴 시작 / 게임 오버에서 메뉴 복귀 |
| 마우스 클릭 · 방향키 · Tab · Enter | 일시정지 중 선택 (모듈 카드 / 함선 부위 / 교체 슬롯) |

### 진행 요약

- **함선 모듈:** XP 요구량 충족 후 **C**. 카드 3장 중 시설 모듈을 골라 범용 슬롯에 장착하거나 **범용 슬롯 +1**을 선택한다.
- **범용 슬롯과 tag:** 시작 **5칸 → 최대 15칸**의 5×3 육각 벌집. 무기실·동력로·엔진·선체·레이더·실드 구분은 장착 제한이 아닌 module tag로 남고, 장착 슬롯 아이콘에 임시 표시된다.
- **모듈 교체:** 범용 슬롯이 꽉 차면 교체 창에서 뺄 슬롯을 지정. 취소하면 오퍼로 복귀. **뺀 모듈 효과는 즉시 사라진다.**
- **무기:** 같은 오퍼에서 획득·전용 모듈을 선택. 필드 무기 드롭과 무기 자체 레벨은 없다. 전용 모듈은 Lv.I~III으로 성장한다. 만석에서 새 무기를 고르면 피교체 무기의 모듈 레벨은 삭제된다.
- **적 강화:** 플레이 시간 약 30초마다. 슬롯 없이 누적.
- **선체·실드:** 좌측 HUD. 실드는 버퍼 HP — 피해는 실드를 먼저 깎고 남는 양만 선체에 들어간다.

### 종료 조건

- 선체(HULL)가 0이 되면 약 1초 후 **게임 오버**.
- 점수·하이스코어 표시 후 스페이스로 메뉴 복귀.
- 새 런은 메뉴에서 다시 시작.

---

## 3. 실행 방법

### A. 브라우저에서 플레이 (권장 · 심사용)

1. 아래 **플레이 링크**를 연다.  
2. 로딩 후 스페이스로 시작한다.

> **플레이 URL:** `TODO: GitHub Pages 웹 빌드 URL`  
> (문서 사이트 https://team-sema.github.io/project-Afterburn/ 과 **별개**일 수 있음)

### B. 소스에서 실행 (개발·검수용)

1. [저장소](https://github.com/team-sema/project-Afterburn)를 clone한다.  
2. **Godot 4.7** stable로 저장소 루트(`project.godot`)를 연다.  
3. `F5` 또는 메인 씬(`menus/menu.tscn`)을 실행한다.

별도 유료 라이선스나 추가 설치(엔진 제외)는 필요하지 않습니다.

---

## 4. 제출 링크

| 항목 | URL |
|------|-----|
| 소스·저장소 | https://github.com/team-sema/project-Afterburn |
| 플레이 (웹 빌드) | `TODO` |
| 플레이 영상 (YouTube, 30~60초) | `TODO` |
| 스펙·칸반 문서 (참고) | https://team-sema.github.io/project-Afterburn/ |

---

## 변경 이력

| 날짜 | 변경 |
|------|------|
| 2026-08-09 | ‘무기 베이’ 표기 → 인게임 ‘무기 모듈’에 맞춤 |
| 2026-08-09 | 개요: 무기 설명 문장 평이하게 정리 |
| 2026-08-09 | 개요: ‘줄타기’ 표현 → 쌍방향 증강 선택 강조 |
| 2026-08-09 | ‘오그먼트’ 표기 → ‘증강’ |
| 2026-07-28 | 초안 작성 |
| 2026-08-01 | 함선 모듈 슬롯·선체·실드 규칙 |
| 2026-08-05 | XP·C 오퍼, 통합 무기 베이, 필드 드롭 제거, 가역 무기 모듈 서술 삭제 |
